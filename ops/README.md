# Tomcat Ops

Tomcat 운영 보조 자료를 두는 공간입니다.

| 폴더 | 내용 |
|------|------|
| `config/` | setenv.sh, systemd, server.xml 운영 샘플 |
| `scripts/` | 배포/전환 자동화 스크립트 예시 |
| `runbooks/` | 재시작, 배포, 장애 대응 절차 |
| `commands/` | 자주 쓰는 점검 명령 모음 |

설정 원리를 설명하는 문서는 `docs/`에 두고, 실제 예시 파일과 운영 보조 자료는 `ops/`에 둡니다.
